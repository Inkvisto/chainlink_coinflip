import { type StoreController } from '@pnpm/store-controller-types';
export declare function createServer(store: StoreController, opts: {
    path?: string;
    port?: number;
    hostname?: string;
    ignoreStopRequests?: boolean;
    ignoreUploadRequests?: boolean;
}): {
    close: () => Promise<void>;
};
