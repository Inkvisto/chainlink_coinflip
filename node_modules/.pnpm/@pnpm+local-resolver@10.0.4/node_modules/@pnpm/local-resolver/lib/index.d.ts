import { type DirectoryResolution, type ResolveResult, type TarballResolution } from '@pnpm/resolver-base';
import { type WantedLocalDependency } from './parsePref';
export type { WantedLocalDependency };
/**
 * Resolves a package hosted on the local filesystem
 */
export declare function resolveFromLocal(wantedDependency: WantedLocalDependency, opts: {
    lockfileDir?: string;
    projectDir: string;
}): Promise<(ResolveResult & Required<Pick<ResolveResult, 'normalizedPref'>> & ({
    resolution: TarballResolution;
} | ({
    resolution: DirectoryResolution;
} & Required<Pick<ResolveResult, 'manifest'>>))) | null>;
