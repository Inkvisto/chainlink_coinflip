import { type Fetchers } from '@pnpm/fetcher-base';
import { type ResolveFunction } from '@pnpm/resolver-base';
import { type ImportIndexedPackage, type StoreController } from '@pnpm/store-controller-types';
export declare function createPackageStore(resolve: ResolveFunction, fetchers: Fetchers, initOpts: {
    engineStrict?: boolean;
    force?: boolean;
    nodeVersion?: string;
    importPackage?: ImportIndexedPackage;
    pnpmVersion?: string;
    ignoreFile?: (filename: string) => boolean;
    cacheDir: string;
    storeDir: string;
    networkConcurrency?: number;
    packageImportMethod?: 'auto' | 'hardlink' | 'copy' | 'clone' | 'clone-or-copy';
    verifyStoreIntegrity: boolean;
}): Promise<StoreController>;
