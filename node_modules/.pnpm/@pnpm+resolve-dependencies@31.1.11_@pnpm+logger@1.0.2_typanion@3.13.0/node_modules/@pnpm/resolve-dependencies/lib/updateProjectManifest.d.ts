import { type ResolvedDirectDependency } from './resolveDependencyTree';
import { type ImporterToResolve } from '.';
export declare function updateProjectManifest(importer: ImporterToResolve, opts: {
    directDependencies: ResolvedDirectDependency[];
    preserveWorkspaceProtocol: boolean;
    saveWorkspaceProtocol: boolean | 'rolling';
}): Promise<(import("@pnpm/types").ProjectManifest | undefined)[]>;
