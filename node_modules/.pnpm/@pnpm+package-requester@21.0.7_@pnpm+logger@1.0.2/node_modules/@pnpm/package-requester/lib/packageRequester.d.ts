import { type Fetchers } from '@pnpm/fetcher-base';
import type { Cafs } from '@pnpm/cafs-types';
import { type ResolveFunction } from '@pnpm/resolver-base';
import { type FetchPackageToStoreFunction, type GetFilesIndexFilePath, type RequestPackageFunction } from '@pnpm/store-controller-types';
export declare function createPackageRequester(opts: {
    engineStrict?: boolean;
    force?: boolean;
    nodeVersion?: string;
    pnpmVersion?: string;
    resolve: ResolveFunction;
    fetchers: Fetchers;
    cafs: Cafs;
    ignoreFile?: (filename: string) => boolean;
    networkConcurrency?: number;
    storeDir: string;
    verifyStoreIntegrity: boolean;
}): RequestPackageFunction & {
    fetchPackageToStore: FetchPackageToStoreFunction;
    getFilesIndexFilePath: GetFilesIndexFilePath;
    requestPackage: RequestPackageFunction;
};
