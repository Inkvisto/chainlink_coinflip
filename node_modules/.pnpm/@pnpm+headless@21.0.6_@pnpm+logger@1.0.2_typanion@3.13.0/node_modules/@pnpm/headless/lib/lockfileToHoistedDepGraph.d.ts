import { type Lockfile } from '@pnpm/lockfile-file';
import { type IncludedDependencies } from '@pnpm/modules-yaml';
import { type PatchFile, type Registries } from '@pnpm/types';
import { type StoreController } from '@pnpm/store-controller-types';
import { type HoistingLimits } from '@pnpm/real-hoist';
import { type LockfileToDepGraphResult } from '@pnpm/deps.graph-builder';
export interface LockfileToHoistedDepGraphOptions {
    autoInstallPeers: boolean;
    engineStrict: boolean;
    force: boolean;
    hoistingLimits?: HoistingLimits;
    externalDependencies?: Set<string>;
    importerIds: string[];
    include: IncludedDependencies;
    ignoreScripts: boolean;
    currentHoistedLocations?: Record<string, string[]>;
    lockfileDir: string;
    nodeVersion: string;
    pnpmVersion: string;
    registries: Registries;
    patchedDependencies?: Record<string, PatchFile>;
    sideEffectsCacheRead: boolean;
    skipped: Set<string>;
    storeController: StoreController;
    storeDir: string;
    virtualStoreDir: string;
}
export declare function lockfileToHoistedDepGraph(lockfile: Lockfile, currentLockfile: Lockfile | null, opts: LockfileToHoistedDepGraphOptions): Promise<LockfileToDepGraphResult>;
