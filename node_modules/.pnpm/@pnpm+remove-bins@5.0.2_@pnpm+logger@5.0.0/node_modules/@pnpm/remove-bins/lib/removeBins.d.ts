import { type DependencyManifest } from '@pnpm/types';
declare function removeOnWin(cmd: string): Promise<void>;
export declare const removeBin: typeof removeOnWin;
export declare function removeBinsOfDependency(dependencyDir: string, opts: {
    dryRun?: boolean;
    binsDir: string;
}): Promise<DependencyManifest | undefined>;
export {};
