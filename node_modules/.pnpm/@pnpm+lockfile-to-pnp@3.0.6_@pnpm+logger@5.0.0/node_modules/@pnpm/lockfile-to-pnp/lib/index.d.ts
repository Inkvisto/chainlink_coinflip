import { type Lockfile } from '@pnpm/lockfile-file';
import { type Registries } from '@pnpm/types';
import { type PackageRegistry } from '@yarnpkg/pnp';
export declare function writePnpFile(lockfile: Lockfile, opts: {
    importerNames: Record<string, string>;
    lockfileDir: string;
    virtualStoreDir: string;
    registries: Registries;
}): Promise<void>;
export declare function lockfileToPackageRegistry(lockfile: Lockfile, opts: {
    importerNames: {
        [importerId: string]: string;
    };
    lockfileDir: string;
    virtualStoreDir: string;
    registries: Registries;
}): PackageRegistry;
