"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = require("fs");
const pathTemp = require("path-temp");
exports.default = async (dir, silent = true) => {
    const tempFile = pathTemp(dir).toLowerCase();
    try {
        await fs_1.promises.writeFile(tempFile, '', 'utf8');
    }
    catch (err) {
        if (silent) {
            return;
        }
        throw err;
    }
    try {
        await fs_1.promises.stat(tempFile.toUpperCase());
        // If the file in different casing is FOUND,
        // then the directory is case insensitive
        return false;
    }
    catch (err) {
        if (err.code === 'ENOENT') {
            // If the file in different casing is NOT FOUND,
            // then the directory is case sensitive
            return true;
        }
        if (silent) {
            return;
        }
        throw err;
    }
    finally {
        // The temp file is removed
        fs_1.promises.unlink(tempFile).catch((err) => {
            // Errors are ignored
        });
    }
};
//# sourceMappingURL=index.js.map