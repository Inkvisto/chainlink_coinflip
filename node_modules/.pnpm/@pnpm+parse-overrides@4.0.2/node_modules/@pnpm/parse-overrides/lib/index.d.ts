export interface VersionOverride {
    parentPkg?: {
        name: string;
        pref?: string;
    };
    targetPkg: {
        name: string;
        pref?: string;
    };
    newPref: string;
}
export declare function parseOverrides(overrides: Record<string, string>): VersionOverride[];
