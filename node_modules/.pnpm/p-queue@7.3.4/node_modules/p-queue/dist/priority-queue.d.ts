import { Queue, RunFunction } from '.pnpm/p-queue@7.3.4/node_modules/p-queue/dist/queue';
import { QueueAddOptions } from '.pnpm/p-queue@7.3.4/node_modules/p-queue/dist/options';
export interface PriorityQueueOptions extends QueueAddOptions {
    priority?: number;
}
export default class PriorityQueue implements Queue<RunFunction, PriorityQueueOptions> {
    #private;
    enqueue(run: RunFunction, options?: Partial<PriorityQueueOptions>): void;
    dequeue(): RunFunction | undefined;
    filter(options: Readonly<Partial<PriorityQueueOptions>>): RunFunction[];
    get size(): number;
}
